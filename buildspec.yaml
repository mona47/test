
version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - cd $CODEBUILD_SRC_DIR
      - ls

artifacts:
  files:
    - IAMROle2.yaml
  base-directory: $CODEBUILD_SRC_DIR
